# Variables de configuración (ejemplo, NO usar estos valores en producción)

# Secreto para firmar JWT (en producción usar un valor fuerte y único)
JWT_SECRET=changeme_replace_with_strong_secret

# Rounds (cost) para bcrypt (mayor => más seguro y más lento). 10-12 es razonable.
BCRYPT_SALT_ROUNDS=10

# Entorno de ejecución: development | production
NODE_ENV=development

# Puerto en el que corre la app dentro del contenedor (coincide con EXPOSE/Docker)
PORT=3000

# Opcional: habilitar migraciones automáticas (scripts/hash-passwords.js) si lo deseas
# WARNING: ejecutar migración sólo si entiendes implicaciones y tienes backup de db.json
MIGRATE_ON_START=false

# Opcional: controles de rate-limit (si implementas)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Opcional: configuración del DB path si necesitas cambiarlo
DB_FILE=db.json